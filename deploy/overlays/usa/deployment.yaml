apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpfull-1778507898
  labels:
    app: cpfull-1778507898
    market: usa
    managed-by: backstage-scaffolder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cpfull-1778507898
  template:
    metadata:
      labels:
        app: cpfull-1778507898
        market: usa
    spec:
      containers:
        - name: app
          # Placeholder until first CI build pushes a real image.
          # `nginx-unprivileged` listens on 8080 (matches Java/Python/Node runtimes).
          # CD pipeline patches `image:` to ghcr.io/priyankaajbhagatt-collab/cpfull-1778507898:<sha>.
          image: 381492075615.dkr.ecr.us-east-1.amazonaws.com/cpfull-1778507898:f9c56780
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
